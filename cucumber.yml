<%
step_definitions = Dir.glob('spec/acceptance/cucumber/step_definitions/**/*.rb').map { |file| "-r #{file}" }.join(" ")
std_opts = "--color --strict --tags ~@draft #{step_definitions}"
%>
default: <%= std_opts %>
progress: --format progress <%= std_opts %>
